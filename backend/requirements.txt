# Ritarinn backend — direct runtime dependencies.
#
# Versions are pinned exactly. Ritarinn is a privacy-first, local-first
# application, so the dependency set is deliberately small and every addition
# should be justified in docs/dependencies-and-licenses.md.
#
# None of these packages contact a network service during normal operation.
# The only outbound connection Ritarinn ever makes is to a loopback address
# (a local Ollama runtime), and that is optional and off the proofreading path.

# --- Icelandic language technology (Miðeind) ---------------------------------
reynir-correct==4.1.3     # GreynirCorrect: spelling + grammar annotations
# Pulled in by reynir-correct, pinned here so the resolved graph is explicit:
reynir==3.8.0             # GreynirEngine: Icelandic parser
islenska==1.3.2           # BinPackage: BÍN morphology
icegrams==1.1.7           # Icelandic trigram model
tokenizer==3.6.4          # Icelandic tokenizer

# --- Web layer ----------------------------------------------------------------
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4

# --- Local-only HTTP client (used solely for 127.0.0.1 Ollama detection) ------
httpx==0.28.1
