# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# Local model weights and caches (never commit model weights).
# Rooted deliberately: an unrooted "models/" also matches
# backend/ritarinn/models/, the Pydantic schema package — which is source.
/models/
*.gguf
*.safetensors
*.bin

# Local configuration and secrets
.env
.env.local
ritarinn.local.toml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
